const BOOLEAN_FALSE = false
const BOOLEAN_TRUE = true
const NULL = null
const ZERO = 0
const ONE = 1
const TWO = 2
const THREE = 3
const FOUR = 4
const FIVE = 5
const SIX = 6
const ONE_THOUSAND = 1000
const DEFAULT_PORT = 3000
const CONSIDER_FIELD = 1
const EXCLUDE_FIELD = 0
const WEB_API_PREFIX = '/web-api'
const SERVER_STATUS = 'server is running'
const DB_CONNECTED = 'Database connected'
const DB_NOT_CONNECTED = 'Error connecting to database: '
const SERVER_STARTED = 'Server is running on port : '
const BAD_REQUEST = 'Something was broken!'
const DATA_FETCHED = 'data fetched successfully'
const UNAUTHORIZED_USER = 'unauthorized user!'
const ACCESS_DENIED = 'Access Denied'
const AUTHORIZATION = 'Authorization'

const STATUS_200 = 200
const STATUS_400 = 400
const STATUS_401 = 401
const STATUS_403 = 403

const DEFAULT_IMAGES_DIR = 'public/images'

const APP = 'app'
const MIDDLEWARE = 'middleware'
const API = 'api'
const AUTH = 'auth'
const SERVICE = 'service'
const AUTHENTICATION = 'authentication'
const CONTROLLER = 'controller'
const S3 = 's3'
const MEDIA = 'media'
const COMMON = 'common'
const UTIL = 'util'
const SECURE = 'secure'

const EX_ONE_MINUTE = 60
const PATH_SEPARATOR = '/'
const EMPTY_STRING = ''

const HIGH_PRIORITY_LOG = 'high'
const LOW_PRIORITY_LOG = 'low'
const INFO_LOG = 'info'
const ERROR_LOG = 'error'
const LOG_TIMESTAMP_FORMAT = 'YYYY-MM-DD HH:mm:ss'

export default {
    BOOLEAN_FALSE,
    BOOLEAN_TRUE,
    NULL,
    ZERO,
    ONE,
    TWO,
    THREE,
    FOUR,
    FIVE,
    SIX,
    ONE_THOUSAND,
    EXCLUDE_FIELD,
    DEFAULT_PORT,
    WEB_API_PREFIX,
    SERVER_STATUS,
    DB_CONNECTED,
    DB_NOT_CONNECTED,
    SERVER_STARTED,
    BAD_REQUEST,
    DATA_FETCHED,
    UNAUTHORIZED_USER,
    ACCESS_DENIED,
    AUTHORIZATION,

    STATUS_200,
    STATUS_400,
    STATUS_401,
    STATUS_403,

    DEFAULT_IMAGES_DIR,

    APP,
    MIDDLEWARE,
    API,
    AUTH,
    SERVICE,
    AUTHENTICATION,
    CONTROLLER,
    S3,
    MEDIA,
    COMMON,
    UTIL,
    SECURE,

    EX_ONE_MINUTE,
    PATH_SEPARATOR,
    EMPTY_STRING,

    HIGH_PRIORITY_LOG,
    LOW_PRIORITY_LOG,
    INFO_LOG,
    ERROR_LOG,
    LOG_TIMESTAMP_FORMAT
}